export const environment = {
  production: true,
  apiUrl: '/api' // URL relativa para produção, assumindo mesmo domínio ou proxy reverso
                 // Ou a URL completa da API de produção: 'https://sua-api.com/api'
};
